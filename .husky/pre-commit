#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fase 1: Hooks leves para não bloquear desenvolvimento
npx lint-staged

# Validação rápida (não bloqueia)
node compliance-engine/continuous-monitoring/daily-status.mjs || true
